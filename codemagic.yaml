scripts:
      - name: Clean and Fix gitignore Encoding
        script: |
          rm -f .gitignore
          printf "node_modules/\n.dart_tool/\n.packages\nbuild/\nios/Podfile.lock\n" > .gitignore
          echo ".gitignore temizlendi."
      - name: Initialize Keychain
        script: keychain initialize
      - name: Fetch Signing Files
        script: |
          # Sadece en temel parametreleri kullanıyoruz
          app-store-connect fetch-signing-files $BUNDLE_ID --create
      - name: Add Certificates to Keychain
        script: keychain add-certificates
      - name: Flutter pub get
        script: flutter pub get
      - name: Pod Install
        script: |
          find . -name "Podfile" -execdir pod install \;
      - name: Flutter build ipa
        script: |
          # Export options kısmını sistemin otomatik yönetmesine bırakıyoruz
          flutter build ipa --release --no-codesign