workflows:
  ios-workflow:
    name: iOS TestFlight Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    integrations:
      app_store_connect: Football Alarm API 
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        APP_STORE_APPLE_ID: 6742335191
        BUNDLE_ID: com.football.alarm 
    scripts:
      - name: Initialize Keychain
        script: |
          # Keychain'i bir şifre ile başlatmak sertifika hatalarını çözer
          keychain initialize
      - name: Fetch and Install Signing Files
        script: |
          # --install parametresi Private Key eşleşmesi için kritiktir
          app-store-connect fetch-signing-files $BUNDLE_ID --create --install
      - name: Add Certificates to Keychain
        script: keychain add-certificates
      - name: Flutter pub get
        script: flutter pub get
      - name: Flutter build ipa
        script: |
          # IPA paketini oluşturur
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true